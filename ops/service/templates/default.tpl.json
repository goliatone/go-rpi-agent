{
  "uuid": "{{.Uuid}}",
       "interfaces: [
    {{range $i, $v := .Interfaces}}{{if $i}},
    {{end}}{ 
      "name": "{{$v.Name}}",
      "value": "{{$v.Value}}",
      "description": "{{$v.Description}}"
    }{{end}}
  ]
}